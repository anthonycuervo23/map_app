import 'package:flutter/material.dart';

class AppDBConstants {
  static const String markersCollection = "markers";
  static const String latitudeDoc = "lat";
  static const String longitudeDoc = "long";
  static const String nameDoc = "name";
}

class AppColors {
  static const titleColor = Colors.black87;
  static const title2Color = Colors.deepOrange;
  static const primaryColor = Color(0xffff414d);
  static const accentColor = Color(0xff1aa687);
}

class AppSizes {
  static const double borderRadius = 10.0;
}
