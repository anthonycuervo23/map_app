import 'package:flutter/material.dart';

class SaveMarkerScreen extends StatelessWidget {
  const SaveMarkerScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SAVE A NEW MARKER'),
      ),
    );
  }
}
